# Backend Environment Configuration Example
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3001

# MongoDB Connection String
# Replace with your MongoDB connection string
MONGO_URI=mongodb://localhost:27017/fullstack-assignment

# JWT Configuration
# Generate a secure random string for production (e.g., using crypto.randomBytes(32).toString('hex'))
JWT_SECRET=your-super-secret-key-change-in-production

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Email Configuration (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-google-app-password
EMAIL_FROM=your-email@gmail.com

# Note: If placeholders are left for EMAIL_USER/PASS, the backend will log links to the console.

